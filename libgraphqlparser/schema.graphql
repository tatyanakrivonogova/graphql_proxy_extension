type Person {
  id: Int!
  name: String!
}

type Message {
  id: Int!
  msg: String!
  sender: Person!
  receiver: Person!
}

type Stat {
  stat: [Int]!
}

type Query {
  getPerson(id: Int! = 9): Person
  person(id: Int!): Person
}

type Mutation {
  createPerson(id: Int! = 8, name: String!): Person
  updatePerson(id: Int!, name: String!): Person
}